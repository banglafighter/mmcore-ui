

// Props: submitUrl, method: [post/get/upload/form], submitSuccess(), submitFailed(), beforeSubmit(), onError(), beforeValidation(), title, subtitle, onCancel, submitLabel, otherButtons, layout = Grid
// NOTE: Conditional show Hide Input, conditional load dropdown